- content_for(:css) do
  = stylesheet_link_tag "admin", :media => "all"
.tabbable
  .biznav
    %ul.nav.nav-tabs.bmenu
      %li.active
        %a{:href => "#1", "data-toggle" => "tab"} All
      %li
        %a{:href => "#2", "data-toggle" => "tab"} Active
      %li
        %a{:href => "#3", "data-toggle" => "tab"} Pending
      %li
        %a{:href => "#4", "data-toggle" => "tab"} Banned
    %ul.navactions
      %li
        %a.btn.btn-primary.btn-mini{:href => new_admin_user_path } Add A New User
    .clear
  - panes = ["users","active","pending","banned"]
  .tab-content
    - 4.times do |i|
      %div{:id => i+1, :class => i+1 == 1 ? "tab-pane active": "tab-pane"}
        %div
          %table.table.table-bordered.table-striped
            = render "tablehead"
            %tbody
              - puts panes[i]
              = render panes[i]
